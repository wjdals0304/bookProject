<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="book.dao.mapper.ZzimMapper">

	<!-- 찜 도서 선택시 저장 -->
	<insert id="create" parameterType="Zzim" > 
	
	</insert>
	
	<!-- 찜 목록 삭제 -->
	<delete id="delete" parameterType="Zzim">
	
	</delete>
	
	<!-- 내 찜목록 조회 리스트 -->
	<select id="retrieveByMemberId" parameterType="int" resultType="Zzim">
	
	</select>
	
</mapper>